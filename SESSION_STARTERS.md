GRG Playscapes — Firebase Studio Session Starter Prompts Version 2.0 | Copy and paste at the start of each Firebase Studio session UNIVERSAL OPENING — PASTE THIS FIRST EVERY SINGLE SESSION  You are building the GRG Playscapes Lumber Inventory App. This is an internal web app for a small team of 5-8 users managing lumber inventory across two locations: Farm and MKE. Before we begin, read and internalize these three documents in the project root: - MASTER_PROMPT.md — what the app does and how it behaves - GEMINI.md — the full technical spec, data schema, and code patterns - BUILD_ORDER.md — the phased implementation plan with checkpoints Critical rules you must follow at all times: 1. NEVER hard-delete Firestore documents — always use the softDelete() utility 2. ALL inventory changes must go through adjustInventory() — never write directly to inventory documents 3. ALL search must use the smartSearch() utility — case insensitive, partial match, space flexible 4. ALL record creation (transfers, quote requests, reconciliation reports) must use generateRecordNumber() in a transaction 5. ALL Firestore queries must include .where('isDeleted', '==', false) unless querying the trash collection 6. Buttons must have minimum 44x44px tap targets throughout 7. Nav bar is at the BOTTOM on mobile, TOP on desktop 8. Every destructive action requires a ConfirmDialog before executing 9. Every popup must have a large X close button in the top-right corner 10. Hi-viz mode must be respected by every component via useTheme() 11. In-transit (inTransitQty) is DISPLAY ONLY — it never reduces inventory quantity 12. All emails are PLAIN TEXT ONLY — no HTML, no attachments, no PDF, no Google Drive uploads We are currently on: PHASE [X] — [PHASE NAME] [Then paste the Phase-Specific Prompt below]  PHASE-SPECIFIC PROMPTS PHASE 0 — PROJECT SETUP  PHASE 0 — PROJECT SETUP Goal: Working skeleton deployed to Firebase Hosting. Nothing needs to work yet — just exist without errors. 1. Install all dependencies from GEMINI.md Section 1 2. Configure Tailwind with GRG brand colors from GEMINI.md Section 1 3. Create full folder structure from GEMINI.md Section 2 — empty files are fine 4. Create Firebase config at src/firebase/config.js using environment variables 5. Set up all .env variables (I will fill in the actual values) 6. Initialize shadcn/ui 7. Create placeholder pages for all 5 main pages 8. Set up React Router with routes for all 5 main pages 9. Deploy to Firebase Hosting — confirm it loads When done, show me the complete folder structure and list all .env variables I need to fill in. Do not proceed to Phase 1.  PHASE 1 — AUTHENTICATION  PHASE 1 — AUTHENTICATION Goal: Only authorized @grgplayscapes.com users can access the app. Session persists forever. Pre-authorized users to seed in the users Firestore collection: - John LaPointe: lapointe@grgplayscapes.com (SUPER_ADMIN) - Dave: dave@grgplayscapes.com (ADMIN) 1. Build SplashPage.jsx — image fade-in 0.5s, text &quot;Press any key to continue&quot; fade-in 0.5s after 2. Implement keypress/tap detection 3. Build Google Sign-In popup on keypress/tap 4. Restrict to @grgplayscapes.com domain via hd parameter 5. Check users Firestore collection — reject emails not found with &quot;Not authorized&quot; error 6. Set Firebase persistence to browserLocalPersistence — never auto logout 7. Build AuthContext.jsx and useAuth.js hook 8. Protect all routes — unauthenticated always lands on Splash 9. Authenticated users skip splash and go directly to Dashboard 10. Seed users collection with John and Dave per GEMINI.md Section 4 Walk me through how to test the login flow when done. Do not proceed to Phase 2.  PHASE 2 — SHELL, NAVIGATION &amp; THEME  PHASE 2 — SHELL, NAVIGATION &amp; THEME Goal: Full visual skeleton. Header, nav, footer, hi-viz, all reusable components working. 1. Build AppHeader.jsx — GRG logo left, current page name center, hi-viz toggle right 2. Build NavBar.jsx — fixed bottom-0 on mobile, md:relative on desktop. Five nav items with simple line-drawing icons and text labels: Dashboard (abstract square), Inventory (cubic box), Products (clipboard), Transfers (truck), More (ellipsis) 3. Build Footer.jsx — three icon-only links using deepLinks.js: Buildertrend, Gmail, Google Drive. Mobile = deep links to native apps, desktop = browser tabs. 4. Build src/utils/deepLinks.js per GEMINI.md Section 14 5. Build ThemeContext.jsx and useTheme.js per GEMINI.md Section 15 6. Implement hi-viz CSS in index.css — pure white bg, pure black text, bold button borders 7. Wire hi-viz toggle to save to users/{uid}.hiVizMode in Firestore and restore on login 8. Build reusable CategoryMenu.jsx — expandable/collapsible, sticky top-0 heading bar, even:bg-[#D9CDB8] alternating rows 9. Build reusable ConfirmDialog.jsx — Cancel and Confirm buttons 10. Build reusable StatusBadge.jsx — Pulled (blue), Pending (gray), In Transit (orange), Complete (green) 11. Build reusable BelowParBadge.jsx — red triangle with exclamation mark Do not proceed to Phase 3.  PHASE 3 — DATA FOUNDATION  PHASE 3 — DATA FOUNDATION Goal: All Firestore collections exist. Core utilities built. 53 SKUs imported. 1. Create all Firestore collections per GEMINI.md Section 4 2. Set Firestore security rules — authenticated @grgplayscapes.com users only 3. Build src/utils/recordNumbers.js per GEMINI.md Section 7 4. Build src/utils/search.js — smartSearch() per GEMINI.md Section 6 5. Build src/utils/inventoryUpdates.js — adjustInventory() per GEMINI.md Section 9 6. Build soft delete utility per GEMINI.md Section 8 7. Seed appSettings/config: physicalCountReminderDays: 35, lastFarmCount: null, lastMkeCount: null 8. Build Admin Control Bulk Import Tool — CSV upload with column validation (required: SKU, Category, Farm Par, MKE Par, Status) 9. On import: write to skus collection + create two inventory documents per SKU (Farm + MKE, qty: 0, inTransitQty: 0) Wait for me to upload the SKU CSV before running the import. Do not proceed to Phase 4.  PHASE 4 — INVENTORY PAGE  PHASE 4 — INVENTORY PAGE Goal: Full inventory page. Users can view, search, and adjust stock at both locations. 1. Build InventoryPage.jsx with smart search bar (use smartSearch()) 2. Build Farm / MKE tab switcher 3. Build useBelowPar.js hook — excludes discontinued SKUs per GEMINI.md Section 11 4. Independent below-par red badge on each tab 5. Three CategoryMenu instances per tab (CDR, CT, GT) from Firestore 6. BelowParBadge on qualifying rows 7. In-transit orange indicator on rows where inTransitQty &gt; 0 — display only, does not affect quantity shown 8. SKU Detail Popup: - Five attributes, location, adjust count +/- - &quot;Save changes?&quot; ConfirmDialog on close if quantity changed - Call adjustInventory() on save - Red trash can → softDelete() + ConfirmDialog 9. Wire below-par badge count to Inventory nav bar icon 10. All queries must include .where('isDeleted', '==', false) Do not proceed to Phase 5.  PHASE 5 — DASHBOARD: MANAGE SKUs &amp; AUDIT LOG  PHASE 5 — DASHBOARD: MANAGE SKUs &amp; AUDIT LOG Goal: SKUs creatable and editable. Full audit log visible and searchable. 1. Build DashboardPage.jsx with four subpage cards: Manage SKUs, Par Report, Physical Count, Audit Log 2. Manage SKUs subpage: - Back arrow, search bar + &quot;Add SKU&quot; button - Add SKU popup: SKU, Category (CDR/CT/GT dropdown), Farm Par, MKE Par, Status, Notes. Cancel + &quot;Create SKU&quot;. - On create: write to skus + two inventory docs (Farm + MKE at qty 0) + log to auditLog - Three CategoryMenus — default collapsed. Sticky header: SKU | Farm Par | MKE Par - Inline editable par levels — save on blur + log to auditLog - BelowParBadge on qualifying rows 3. Audit Log subpage: - Back arrow, smart search bar - Filter buttons: Stock Adjustment | Transfer | Pick | Physical Count | Item Created | Item Deleted | Material Added | Transfer Discrepancy - Chronological list: event | SKU | reason | date | user - Export button — hidden md:flex (desktop only) — CSV and PDF of current results Do not proceed to Phase 6.  PHASE 6 — VENDORS, USER MANAGEMENT &amp; ADMIN CONTROL  PHASE 6 — VENDORS, USER MANAGEMENT &amp; ADMIN CONTROL Goal: Vendors and users manageable. Admin Control wired and role-gated. 1. Build MorePage.jsx with 8 subpage cards. Admin Control card only visible when user.email === 'lapointe@grgplayscapes.com' or 'dave@grgplayscapes.com' 2. Vendor Contacts subpage: - &quot;Add +&quot; → popup: Contact Name, Company, Email, Phone (optional). &quot;Add Vendor&quot; button. - Vendor list with soft delete + ConfirmDialog 3. User Management subpage: - &quot;Add User +&quot; → popup: Email, Name, Role dropdown (SUPER_ADMIN/ADMIN/FIELD_CREW) - User list with edit/deactivate - John (lapointe@) edits anyone. Dave (dave@) edits anyone except John. No one else edits either. 4. Admin Control subpage — invisible to everyone except John and Dave: - Soft Delete Restore Center: list trash collection, Restore + Permanent Delete per item - Bulk Import Tool UI (already built Phase 3) - Physical count threshold: number field → appSettings/config.physicalCountReminderDays - Role permission toggles — all OFF by default. Build RoleGate component per GEMINI.md Section 16. - John-only section (invisible to Dave): manage Dave's account — demote, suspend, remove Do not proceed to Phase 7.  PHASE 7 — PAR REPORT &amp; QUOTE REQUESTS  PHASE 7 — PAR REPORT &amp; QUOTE REQUESTS Goal: Below-par items visible by location. Quote requests generated and emailed as plain text. 1. Par Report subpage: - Back arrow - Farm section + MKE section - Active below-par SKUs only (discontinued excluded): SKU | qty below par | par | &quot;Quote Request&quot; button - &quot;Generate Quote Request&quot; button appears when ≥1 selected - Wire below-par red badge to Par Report subpage card on Dashboard 2. Generate Quote Request popup: - Auto-generated QR# via generateRecordNumber('quoteRequests') - Date auto-populated, Vendor dropdown from vendors collection - Selected SKUs with +/- qty selectors, notes, Cancel + &quot;Send Quote Request&quot; 3. On send: - Write to quoteRequests collection + log to auditLog - Trigger email Cloud Function — plain text to vendor email AND configurable internal recipient. NO attachments. 4. Quote Requests subpage under More: - Chronological list: QR# | date | vendor | SKU count | sent by - Tap → full detail popup Do not proceed to Phase 8.  PHASE 8 — TRANSFERS  PHASE 8 — TRANSFERS Goal: Full transfer lifecycle. inTransitQty is display only — inventory only updates on receive. 1. TransfersPage.jsx — search bar + &quot;NEW +&quot;, list newest first 2. New Transfer popup: - Auto-generated T# via generateRecordNumber('transfers') - From dropdown → To auto-populates opposite location - Smart SKU search (smartSearch()) placeholder &quot;search SKU…&quot; - Selected SKUs with large +/- qty buttons, date, notes, &quot;Request Transfer&quot; 3. Transfer list: direction | SKU count | date | StatusBadge | &quot;View SKUs&quot; button | action button | trash 4. &quot;View SKUs&quot; popup — lists all SKUs and quantities 5. Ship flow — &quot;Confirm Shipment&quot; popup: - On confirm: status → in_transit; set inventory.inTransitQty += requestedQty on &quot;from&quot; docs (DO NOT call adjustInventory — display only); lock transfer (hide edit UI when status !== 'requested'); log to auditLog; console.log &quot;EMAIL TRIGGERED: Transfer Shipped T#XXXX&quot; (real email wired Phase 11) 6. Wire in-transit orange indicator on Inventory page — reads inTransitQty &gt; 0 7. Carpenter pull warning: if product SKU has inTransitQty &gt; 0, show warning dialog. Do NOT block the pull. 8. Receive flow — &quot;Receive Transfer&quot; popup: - Editable qty fields pre-filled with requestedQty - If any qty differs: set transfer.discrepancyFlagged = true, log TRANSFER_DISCREPANCY to auditLog, console.log &quot;NOTIFICATION TRIGGERED: Discrepancy T#XXXX&quot; - On confirm: call adjustInventory() negative delta on &quot;from&quot; + positive delta on &quot;to&quot; using receivedQty; clear inTransitQty on &quot;from&quot; docs; status → received; log to auditLog; console.log &quot;EMAIL TRIGGERED: Transfer Received T#XXXX&quot; 9. Delete — softDelete() + ConfirmDialog — only when status === 'requested' Do not proceed to Phase 9.  PHASE 9 — PRODUCTS &amp; PRODUCT LIBRARY  PHASE 9 — PRODUCTS &amp; PRODUCT LIBRARY Goal: Full product lifecycle from CSV upload through pulling to completion. 1. ProductsPage.jsx — search bar + &quot;NEW +&quot;, list newest first 2. CSV upload popup — validate required columns: Catalogue ID, Product Name, SKU, QTY. Reject if missing. 3. Complete Product Setup popup: - CSV preview, Product Name, Catalog ID, Location dropdown (Farm/MKE), Job Name (type new or select existing from jobs collection) - Out-of-stock items in red text. &quot;Create Product&quot; → write to products + jobs (if new) + productLibrary + log to auditLog 4. Individual product page: - Back arrow, product name, job name, location - Red trash can — softDelete() + ConfirmDialog + restore all pulled inventory via adjustInventory() - &quot;Add Material +&quot; — log to auditLog - Materials list: SKU | QTY | checkbox - &quot;Submit Pulled&quot; when ≥1 checked - On pull: adjustInventory() negative delta; blue Pulled badge + X undo button; log to auditLog - On undo (X): adjustInventory() positive delta; remove badge; log to auditLog - When ALL pulled: green &quot;Complete&quot; button appears - On Complete: ConfirmDialog → status = complete → hidden from Products page → console.log &quot;EMAIL TRIGGERED: Product Complete&quot; → log to auditLog 5. Product list: all pulled → blue Pulled badge; some/none → gray Pending; complete → hidden 6. Product Library subpage — read-only list. &quot;Assign to Job +&quot; per row → popup: Location + Job Name → fresh products document created 7. Projects subpage — list with product counts; tap → popup with products + StatusBadges; soft delete project + ConfirmDialog Do not proceed to Phase 10.  PHASE 10 — PHYSICAL COUNT &amp; RECONCILIATION REPORTS  PHASE 10 — PHYSICAL COUNT &amp; RECONCILIATION REPORTS Goal: Physical counts work. Reports store in app and trigger plain text email notification. 1. Physical Count subpage: - Back arrow, &quot;Select a Location&quot; dropdown (Farm/MKE) - On selection: smart search + three collapsed CategoryMenus (CDR, CT, GT) - SKU rows: name | number input +/- | default 0 - &quot;Submit Reconciliation Report&quot; appears when ≥1 SKU has non-zero count 2. Submit Reconciliation Report popup: - Required: user name, count date - Auto-populated: location, discrepancy list (systemQty vs countedQty) - Cancel + &quot;Submit Report&quot; 3. On submit: - Call adjustInventory() for each counted item: delta = countedQty - systemQty - Write to reconciliationReports with RC# via generateRecordNumber('reconciliationReports') - Update appSettings/config lastFarmCount or lastMkeCount - Log to auditLog - console.log &quot;EMAIL TRIGGERED: Reconciliation Report RC#XXXX&quot; (real email wired Phase 11) 4. Reconciliation Reports subpage under More: - List: RC# | location | date | submitted by - Two expandable sections: Farm | MKE - Tap → detail popup: discrepancies + below-par items at time of count - Desktop-only download button (hidden md:flex) — PDF of report Do not proceed to Phase 11.  PHASE 11 — NOTIFICATIONS &amp; EMAIL SYSTEM  PHASE 11 — NOTIFICATIONS &amp; EMAIL SYSTEM Goal: All notification events trigger correctly. Plain text emails via sales@grgplayscapes.com. 1. Notifications subpage: - &quot;Add +&quot; → popup: Name, Email, Notification Types checklist - Full event list per MASTER_PROMPT.md Section 9 - Each event independently assignable to any team members 2. Build triggerNotification(eventType, data) Cloud Function per GEMINI.md Section 12 3. Build sendEmail() via SendGrid — plain text ONLY, from sales@grgplayscapes.com, NO attachments 4. Replace ALL console.log &quot;EMAIL TRIGGERED&quot; and &quot;NOTIFICATION TRIGGERED&quot; placeholders from Phases 7–10 with real Cloud Function calls 5. Build dailyMaintenance scheduled Cloud Function per GEMINI.md Section 13: - Purge trash older than 30 days - Auto-delete discontinued SKUs at zero quantity + log + notify - Check physical count overdue per location against threshold 6. Test every single notification event end-to-end — confirm plain text email received at sales@grgplayscapes.com Do not proceed to Phase 12.  PHASE 12 — BELOW-PAR BADGE WIRING  PHASE 12 — BELOW-PAR BADGE WIRING Goal: Every below-par badge wired, accurate, updates in real time. Wire useBelowPar() hook to all badge locations: 1. Nav bar Inventory icon — total below-par count across BOTH locations 2. Dashboard Par Report subpage card — total count 3. Inventory Farm tab — Farm-only count 4. Inventory MKE tab — MKE-only count 5. Each CategoryMenu header (CDR, CT, GT) per location — small red dot if ANY SKU in that category is below par 6. Individual SKU rows — BelowParBadge component Rules: - Discontinued SKUs NEVER appear in any below-par count - Badges update immediately on inventory change — no page refresh - Computed from useBelowPar() hook only — never calculated inline in components Test: manually adjust a SKU below par and confirm all badges update across the entire app immediately. Do not proceed to Phase 13.  PHASE 13 — FULL QA &amp; POLISH  PHASE 13 — FULL QA &amp; POLISH Goal: Production-ready. Every flow tested. Every edge case handled. Work through the full QA test matrix in BUILD_ORDER.md Phase 13 systematically. Fix each failure before moving to the next item. After the test matrix, complete these polish items: 1. Every popup has large X close button top-right 2. Every destructive action has ConfirmDialog 3. All CategoryMenu sticky headers scroll correctly on mobile and desktop 4. Empty states have friendly messages everywhere (no transfers yet, no vendors, etc.) 5. Error states have clear user-facing messages 6. Loading indicators while Firestore data loads 7. Test on real mobile device — not just DevTools simulation When all items pass confirm we are ready for Phase 14.  PHASE 14 — LAUNCH  PHASE 14 — LAUNCH Goal: App live in production. Team onboarded. 1. Final production build: npm run build 2. Deploy: firebase deploy 3. Confirm live URL works — all features functional in production 4. Confirm all Cloud Functions active in Firebase Console 5. Confirm Firestore security rules active in production Provide step-by-step instructions for me to do manually: A. Add remaining team members in User Management B. Configure notification recipients per event type C. Add vendor contacts D. Verify SendGrid → sales@grgplayscapes.com is working Also provide a one-page Team Quick Start Guide I can share with field crew covering: - How to install the app on iPhone and Android (PWA) - How to log in - How to pull materials for a product - How to do a physical count - What the hi-viz toggle does  MID-SESSION RECOVERY PROMPT Use when Gemini loses context or starts breaking things  Stop. Reset context. We are building the GRG Playscapes Lumber Inventory App. Read GEMINI.md and MASTER_PROMPT.md in the project root. We are on Phase [X] — [PHASE NAME]. Last thing that was working: [describe] Current problem: [describe] Do not change anything that was already working. Fix only the specific issue described above.  QUICK REFERENCE — CRITICAL PATTERNS Paste if Gemini ever forgets a core rule  Non-negotiable patterns for the GRG Lumber App: SOFT DELETE — never doc.ref.delete() directly: await softDelete(collectionName, docId, userId, userName); INVENTORY — always use the utility: await adjustInventory({ skuId, location, delta, reason, relatedId, userId, userName }); IN-TRANSIT — display only, NEVER reduces quantity: inventory.inTransitQty += qty // set on ship inventory.inTransitQty = 0 // clear on receive // adjustInventory() is NOT called on ship — only on receive RECORD NUMBERS — always in a transaction: const number = await generateRecordNumber('transfers'); // options: 'transfers' | 'quoteRequests' | 'reconciliationReports' SEARCH — always use the utility: const results = smartSearch(items, query, ['sku', 'category']); FIRESTORE QUERIES — always exclude soft-deleted: .where('isDeleted', '==', false) BUTTONS — minimum tap target: className=&quot;min-h-[44px] min-w-[44px]&quot; NAV: Mobile: fixed bottom-0 Desktop: md:relative md:top-auto md:bottom-auto EXPORT BUTTONS — desktop only: className=&quot;hidden md:flex&quot; EMAIL — plain text only: No HTML. No attachments. No PDFs. No Google Drive. Plain text via sales@grgplayscapes.com only. HI-VIZ — every component must respect theme: const { hiViz } = useTheme();  End of Session Starter Prompts v2.0 GRG Playscapes Lumber Inventory App Prepared by Dave &amp; Claude | February 2026  